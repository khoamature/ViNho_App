<?xml version="1.0" encoding="utf-8"?>
<!-- 1. DrawerLayout không cần fitsSystemWindows, để các view con tự xử lý -->
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1E1F20"
    android:clickable="true"
    android:focusable="true"
    tools:context=".ui.fragments.ChatbotFragment"
    tools:openDrawer="start">

    <!-- 2. Nội dung chính của màn hình chat - Thêm fitsSystemWindows="true" -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <!-- Header của màn hình chat -->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:elevation="0dp">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:contentInsetStart="0dp">

                <!-- SỬA LẠI: Nút Back thay cho nút Menu -->
                <ImageView
                    android:id="@+id/btnOpenDrawer"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginStart="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="12dp"
                    android:src="@drawable/ic_menu"
                    app:tint="@color/white" />

            </androidx.appcompat.widget.Toolbar>
        </com.google.android.material.appbar.AppBarLayout>

        <!-- RecyclerView để hiển thị các tin nhắn -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_chat_messages"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/layout_input_area"
            android:layout_below="@id/appBarLayout"
            android:padding="8dp" />

        <!-- Welcome Message -->
        <TextView
            android:id="@+id/tv_welcome_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:padding="32dp"
            android:text="Hello! I am Vi Nho Financial Assistant!\nWhat can I help you?"
            android:textColor="#CCCCCC"
            android:textSize="22sp"
            android:visibility="visible" />

        <!-- Khu vực nhập liệu -->
        <LinearLayout
            android:id="@+id/layout_input_area"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="8dp">

            <EditText
                android:id="@+id/edit_text_message"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/bg_chat_input"
                android:hint="Type your message..."
                android:maxLines="4"
                android:paddingHorizontal="16dp"
                android:paddingVertical="12dp"
                android:textColor="@color/white"
                android:textColorHint="#9E9E9E" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btn_send_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_send"
                app:backgroundTint="#4A4A4A"
                app:fabSize="mini"
                app:tint="@color/white" />
        </LinearLayout>
    </RelativeLayout>

    <!-- 3. GIAO DIỆN CỦA MENU BÊN TRÁI - Thêm fitsSystemWindows="true" -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#1E1F20"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_drawer_header"
        app:itemIconTint="@color/white"
        app:itemTextColor="@color/white"
        app:menu="@menu/chatbot_history_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
